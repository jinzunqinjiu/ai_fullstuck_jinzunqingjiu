/* 模块化 */
.spec {
  /* 新的格式化上下文 */
display:flex;
border-bottom: 1px solid #ededed;
font-size: 24rpx;
}
.spec .item {
  border-right: 1px solid #ededed;
  padding: 64rpx;
  flex: 1;
  /* 子元素里设置多个子元素的flex比例值 */
  /* width: 50vh/vw; */
  /* 以上都可以将二列式平分 */
}
/* spex模块 */
.spec .item .value text { 
  font-size: 60rpx;
  margin-right: 8rpx;
}